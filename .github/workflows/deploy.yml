name: Deploy to GitHub Pages

on:
  push:
    branches: ['main']
    # Trigger on changes to example files in parsanol-rs or parsanol-ruby
    paths:
      - 'src/**'
      - 'scripts/**'
      - 'package.json'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Fetch sibling repos for examples
          repository: parsanol/parsanol-rs
          path: ../parsanol-rs
          sparse-checkout: |
            examples
          sparse-checkout-cone-mode: false

      - name: Checkout parsanol-ruby
        uses: actions/checkout@v4
        with:
          repository: parsanol/parsanol-ruby
          path: ../parsanol-ruby
          sparse-checkout: |
            example
          sparse-checkout-cone-mode: false

      - name: Update paths in generate script
        run: |
          sed -i "s|/Users/mulgogi/src/parsanol/parsanol-rs/examples|../parsanol-rs/examples|g" scripts/generate-examples.cjs
          sed -i "s|/Users/mulgogi/src/parsanol/parsanol-ruby/example|../parsanol-ruby/example|g" scripts/generate-examples.cjs

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
